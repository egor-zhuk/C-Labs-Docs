## Тема: Использование окон диалога в формах

**Выполнил:** Студент группы ИУ10-19 Жуков Георгий Михайлович
**Дата:** 2025 год, Москва

### Цель работы
Изучение способов использования компонентов, представляющие диалоговые окна и получение навыков по работе с окнами диалога.

### Ход работы

#### Упражнение 1. Использование компонента SaveFileDialog
В форме `Form9` (текстовый редактор) добавлено меню "Файл" -> "Сохранить как...". Используется компонент `SaveFileDialog` для выбора места сохранения файла.

**Код (`Form9.cs`):**
```csharp
private void saveAsMenuItem_Click(object sender, EventArgs e)
{
    saveFileDialog1.Filter = "txt files (*.txt)|*.txt|All files (*.*)|*.*";
    
    if (saveFileDialog1.ShowDialog() == DialogResult.OK && saveFileDialog1.FileName.Length > 0)
    {
        richTextBox1.SaveFile(saveFileDialog1.FileName, RichTextBoxStreamType.PlainText);
    }
}
```

**Результат:**
> ![[Pasted image 20251218130137.png]]

#### Упражнение 2. Использование компонента ColorDialog
Добавлено меню "Формат" -> "Цвет фона...". Используется `ColorDialog` для выбора цвета фона текстового поля `RichTextBox`.

**Код (`Form9.cs`):**
```csharp
private void colorMenuItem_Click(object sender, EventArgs e)
{
    if (colorDialog1.ShowDialog() == DialogResult.OK)
    {
        richTextBox1.BackColor = colorDialog1.Color;
    }
}
```

**Результат:**
> ![[Pasted image 20251218130205.png]]

#### Упражнение 3. Использование компонента FontDialog
Добавлено меню "Формат" -> "Шрифт...". Используется `FontDialog` для изменения шрифта текста.

**Код (`Form9.cs`):**
```csharp
private void fontMenuItem_Click(object sender, EventArgs e)
{
    if (fontDialog1.ShowDialog() == DialogResult.OK)
    {
        richTextBox1.Font = fontDialog1.Font;
    }
}
```

**Результат:**
> ![[Pasted image 20251218130224.png]]

#### Упражнение 4. Использование компонента OpenFileDialog
Добавлено меню "Файл" -> "Открыть...". Используется `OpenFileDialog` для открытия текстовых файлов.

**Код (`Form9.cs`):**
```csharp
private void openMenuItem_Click(object sender, EventArgs e)
{
    openFileDialog1.Filter = "txt files (*.txt)|*.txt|All files (*.*)|*.*";
    openFileDialog1.FilterIndex = 1; 

    if (openFileDialog1.ShowDialog() == DialogResult.OK)
    {
        try 
        {
            Stream myStream;
            if ((myStream = openFileDialog1.OpenFile()) != null)
            {
                using (myStream)
                {
                    richTextBox1.LoadFile(openFileDialog1.FileName, RichTextBoxStreamType.PlainText);
                }
            }
        }
        catch (Exception ex)
        {
            MessageBox.Show("Error: Could not read file from disk: " + ex.Message);
        }
    }
}
```

**Результат:**
> ![[Pasted image 20251218130250.png]]

### Выводы
Приобретены навыки работы со стандартными диалоговыми окнами Windows Forms: `SaveFileDialog`, `OpenFileDialog`, `ColorDialog` и `FontDialog`. Реализован простейший текстовый редактор с функциями открытия, сохранения и форматирования текста.