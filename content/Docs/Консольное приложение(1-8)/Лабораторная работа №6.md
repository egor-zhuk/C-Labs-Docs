## Тема: Создание и использование классов

**Выполнил:** Студент группы ИУ10-19 Жуков Георгий Михайлович
**Дата:** 2025 год, Москва

### Цель работы
Изучение понятия класса как пользовательского типа данных и приобретение навыков работы с классами.

### Ход работы

#### Упражнение 1. Создание класса
Структура `BankAccount` преобразована в класс `SberBankAccount`. Добавлены методы для инициализации (`Populate`) и доступа к данным.

#### Упражнение 2. Генерация номеров счетов
В класс добавлено статическое поле `nextAccNo` и метод `NextNumber` для автоматической генерации уникальных номеров счетов.

**Код:**
```csharp
private static long nextAccNo;
private static long NextNumber()
{
    nextAccNo++;
    return nextAccNo;
}
```

#### Упражнение 3. Добавление методов Deposit и Withdraw
Реализованы методы для пополнения и снятия средств с проверкой достаточности баланса.

**Код:**
```csharp
public decimal Deposit(decimal amount)
{
    accBal += amount;
    return accBal;
}
public bool Withdraw(decimal amount)
{
    bool sufficientFunds = accBal >= amount;
    if (sufficientFunds) accBal -= amount;
    return sufficientFunds;
}
```

**Результат:**
> ![[Pasted image 20251218122911.png]]

### Выводы
Изучены основы объектно-ориентированного программирования в C#: создание классов, инкапсуляция данных, использование статических полей и методов.

---

