# Лабораторная работа №8
## Тема: Создание объектов и управление ресурсами

**Выполнил:** Студент группы ИУ10-19 Жуков Георгий Михайлович
**Дата:** 2025 год, Москва

### Цель работы
Изучение конструкторов при создании объектов и приобретение навыков работы с шаблонами для удаления объектов.

### Ход работы

#### Упражнение 1. Разработка конструкторов
В класс `TBankAccount` добавлены перегруженные конструкторы для инициализации счета различными способами.

**Код:**
```csharp
public TBankAccount()
{
    accNo = NextNumber();
    accType = AccountType.Checking;
    accBal = 0;
}
public TBankAccount(AccountType aType)
{
    accNo = NextNumber();
    accType = aType;
    accBal = 0;
}
public TBankAccount(decimal aBal)
{
    accNo = NextNumber();
    accType = AccountType.Checking;
    accBal = aBal;
}
public TBankAccount(AccountType aType, decimal aBal)
{
    accNo = NextNumber();
    accType = aType;
    accBal = aBal;
}
```

#### Упражнение 2. Инициализация данных только для чтения
Создан класс `BankTransaction` для хранения информации о транзакциях. В класс `TBankAccount` добавлена очередь `Queue` для хранения истории операций.

**Код:**
```csharp
public class BankTransaction
{
    private readonly decimal amount;
    private readonly DateTime when;
    public BankTransaction(decimal tranAmount)
    {
        amount = tranAmount;
        when = DateTime.Now;
    }

    public decimal Amount()
    {
        return amount;
    }
    public DateTime When()
    {
        return when;
    }
}


private Queue tranQueue = new Queue();

public decimal Deposit(decimal amount)
{
    accBal += amount;
    BankTransaction tran = new BankTransaction(amount);
    tranQueue.Enqueue(tran);
    return accBal;
}
public bool Withdraw(decimal amount)
{
    bool sufficientFunds = accBal >= amount;
    if (sufficientFunds)
    {
        accBal -= amount;
        BankTransaction tran = new BankTransaction(-amount);
        tranQueue.Enqueue(tran);
    }
    return sufficientFunds;
}
public Queue Transactions()
{
    return tranQueue;
}
```

#### Упражнение 3. Использование шаблона для удаления объектов
Класс `TBankAccount` реализует интерфейс `IDisposable`. Метод `Dispose` сохраняет данные о транзакциях в файл и освобождает ресурсы.

**Код:**
```csharp
public void Dispose()
{
    if (!disposed)
    {
        StreamWriter swFile = File.AppendText("Transactions.Dat");
        swFile.WriteLine("Account number is {0}", accNo);
        swFile.WriteLine("Account balance is {0}", accBal);
        swFile.WriteLine("Account type is {0}", accType);
        swFile.WriteLine("Transactions:");
        foreach (BankTransaction tran in tranQueue)
        {
            swFile.WriteLine("Date/Time: {0}\tAmount:{1}", tran.When(),
            tran.Amount());
        }
        swFile.Close();
        disposed = true;
        GC.SuppressFinalize(this);
    }
}
```

#### Тестирование работы (Метод Lab08)
Код, демонстрирующий создание счетов, выполнение операций и автоматическое освобождение ресурсов с помощью `using`.

**Код:**
```csharp
private void Lab08()
{
    Console.WriteLine("Лабораторная работа 8 упражнение 1:\n");
    TBankAccount acc1, acc2, acc3, acc4;
    acc1 = new TBankAccount();
    acc2 = new TBankAccount(AccountType.Deposit);
    acc3 = new TBankAccount(100);
    acc4 = new TBankAccount(AccountType.Deposit, 500);
    Write(acc1);
    Write(acc2);
    Write(acc3);
    Write(acc4);

    Console.WriteLine("\n\nЛабораторная работа 8 упражнение 2:\n");
    acc1.Deposit(100);
    acc1.Withdraw(50);
    acc1.Deposit(75);
    acc1.Withdraw(50);
    acc1.Withdraw(30);
    acc1.Deposit(40);
    acc1.Deposit(200);
    acc1.Withdraw(250);
    acc1.Deposit(125);
    TWrite(acc1);

    Console.WriteLine("\n\nЛабораторная работа 8 упражнение 3:\n");
    using (acc1 = new TBankAccount())
    {
        acc1.Deposit(100);
        acc1.Withdraw(50);
        acc1.Deposit(75);
        acc1.Withdraw(50);
        acc1.Withdraw(30);
        acc1.Deposit(40);
        acc1.Deposit(200);
        acc1.Withdraw(250);
        acc1.Deposit(25);
        Write(acc1);
    }
}
```

**Результат:**
> ![[Pasted image 20251218123539.png]]
> ![[Pasted image 20251218123550.png]]

### Выводы
Изучены конструкторы и их перегрузка для гибкой инициализации объектов. Освоена работа с неизменяемыми полями (`readonly`). Реализован интерфейс `IDisposable` и шаблон `Dispose` для корректного освобождения ресурсов (записи в файл) при уничтожении объекта.